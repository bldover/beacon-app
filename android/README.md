# beacon-android
Android app frontend for the concert tracker/recommender

Build variant variables should be populated in flavor.<env>.properties for the variant, 
following the flavor.env.properties template